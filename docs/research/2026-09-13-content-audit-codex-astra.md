> 생성: 2026-09-13, Codex CLI 0.154.0 / gpt-6-astra (read-only 리포 분석). Claude(Advisor) 브리프 기반 콘텐츠·카피 감사. 원문 보존. 채택 여부·브랜드 결정은 별도.

**Nunchi 콘텐츠·카피 감사 — 2026-09-13**

파일 수정 없이 지정 문서·UI·백엔드를 대조했다. **우선 수정 대상은 수치·속도·개인정보 설명이며, 표현 리스크는 현재 제공 기능으로 홍보하기 어렵다.** 사전 68개가 모두 `draft`여서 기본 설정의 활성 항목은 0개다.

사건 JSON은 `_template.json` 제외 **50개**, 실제 사용 범주는 **10개**다. 운영 중단·현재 모델·10–20초 지연은 Claude가 제공한 당일 확인값이며 여기서 재측정하지 않았다. README는 현재 구현의 근거로 사용하지 않았다. 아래 인용은 실제 문자열의 필요한 부분만 발췌했다. `{BRAND}`는 소유자 결정 후 치환할 자리다.

**1. 사실 오류·과장 표**

| 위치 | 현재 문구 | 판정·근거 | 제안 |
|---|---|---|---|
| `apps/web/app/(landing)/page.tsx:29,45,47,344,674` | “60+”, “한국 사건 60+ DB” | **오류:** 원본 50건 | “50건”, “한국 사건·기념일 50건” |
| `apps/web/app/layout.tsx:29,47,52`; `apps/web/app/check/page.tsx:75,240`; `apps/web/app/events/page.tsx:11`; `apps/web/components/SiteFooter.tsx:149`; `apps/web/app/opengraph-image.tsx:128` | “60+”, “60개+” | **오류:** 같은 수치가 메타·본문·공유 이미지에 반복 | “50건”. 장기적으로 승인된 데이터 건수와 연동 |
| `apps/web/app/(landing)/page.tsx:30,38,54,346`; `apps/web/app/layout.tsx:29,52`; `apps/web/app/check/page.tsx:74,227`; `apps/web/app/events/[slug]/page.tsx:230`; `apps/web/components/SiteFooter.tsx:149` | “5초”, “5초 안에”, “보통 5–10초” | **오류:** LLM 경로 10–20초. 룰 경로 속도를 전체에 적용할 수 없음 | 본문 “캠페인 사전 검토”; 대기 화면 “AI 분석에는 약 10–20초가 걸리며, 요청에 따라 더 길어질 수 있습니다.” |
| `apps/web/app/(landing)/page.tsx:309`; `apps/web/app/check/page.tsx:240` | “AI가 즉시 분석”, “즉시 교차 분석” | 전체 요청의 즉시 응답은 보장 불가 | “관련 사건과 카피를 교차 검토합니다.” |
| `apps/web/app/(landing)/page.tsx:674` | “Gemini LLM” | **현재 구성과 불일치.** `packages/llm/src/claude.ts:24–27`은 키 설정에 따라 공급자 선택 | “사건 데이터와 규칙·AI 분석을 결합합니다.” |
| `apps/web/app/(landing)/page.tsx:53,495–497` | “단순 단어 매칭이 아닙니다”, “맥락을 이해합니다” | 룰 적중 시 LLM·날짜 검토를 생략함: `apps/web/lib/review-engine.ts:235–251` | “등록된 키워드를 확인하고, 필요한 경우 AI로 관련 맥락을 검토합니다.” |
| `apps/web/app/(landing)/page.tsx:39,60,686` | “안전 카피·적합 일자 자동 제안”, “대안이 함께 제시됩니다” | **보장 불가:** 룰 F는 `suggestions: []`. 날짜 버튼은 적합성 평가 없이 +1·7·14일: `apps/web/components/result-card/ResultCard.tsx:748–755` | “결과에 따라 대안을 제안합니다. 다른 날짜도 다시 검토할 수 있습니다.” |
| `apps/web/app/(landing)/page.tsx:678` | “5가지 카테고리”, “국가 추모일(…6·25, 10·29 등)” | **분류 수 오류:** 스키마·실제 데이터 모두 10범주. ‘국가 추모일’은 공식 지정 여부를 혼동시킴 | “역사·사회 사건, 추모 관련 날짜, 국가 기념일과 상업 기념일을 다룹니다.” |
| `apps/web/app/(landing)/page.tsx:68,106,113,120,122` | “현충일·1·22 전후”, 연도 없는 요일, “매출 호재” | 1·22의 사건 근거, 요일 기준 연도, 매출 효과 **(확인 필요)** | 1·22 삭제; 요일 삭제; “기념일 연관성” |
| `apps/web/app/(landing)/page.tsx:81,89,97,613,646` | 고정 F/D/A, “클릭 한 번으로 실제 검토 결과”, “검토 결과 보기” | 등급은 정적 예시. 링크는 폼만 채움: `apps/web/app/check/page.tsx:15–18,43–54` | “예시 등급”; “예시를 불러온 뒤 검토를 시작할 수 있습니다”; “이 예시로 검토하기” |
| `apps/web/app/(landing)/page.tsx:32,690,781`; `apps/web/app/contact/page.tsx:43`; `apps/web/app/terms/page.tsx:13` | “베타 기간 무료”, “현재…운영 중”, “지금 베타 무료” | **운영 상태 오류:** 공개 사이트는 현재 오프라인. 무료 정책 자체는 별도 확인 대상 | 현재 안내 “현재 공개 서비스는 중단되어 있습니다. 재개 일정과 이용 조건은 추후 안내합니다.” |
| `apps/web/app/(landing)/page.tsx:324,791`; `apps/web/app/events/[slug]/page.tsx:233`; `apps/web/components/AppHeader.tsx:167,298` | “무료로…” | 결제 미구현은 확인되지만 지속적인 무료 제공 약속의 근거는 아님. 헤더는 실제로 Google 로그인 실행: `AppHeader.tsx:41` | 랜딩 “검토 화면 보기”; 헤더 “Google로 로그인” |
| `apps/web/app/(landing)/page.tsx:690,694`; `apps/web/app/contact/page.tsx:55` | 베타 사용자 혜택, 국가별 확장 순서, “API 플랜 제공 예정” | 확정 정책·일정 **(확인 필요)** | “요금제와 추가 지원 범위는 확정 후 안내합니다.” |
| `apps/web/app/contact/page.tsx:124`; `apps/web/app/privacy/page.tsx:50` | “보통 1영업일”, “1-2영업일” | 서로 다름. 응답 실적·운영 약속 **(확인 필요)** | “접수한 문의는 이메일로 답변드립니다.” |
| `apps/web/app/onboarding/page.tsx:100`; `apps/web/app/mypage/page.tsx:107` | “정밀하게 분석”, “모든 검토 결과…즉시 반영” | 업종·채널로 추가 코멘트만 생성. 기존 결과·등급을 갱신하지 않음: `apps/web/app/api/check/route.ts:49–65` | “업종·채널을 저장하면 다음 검토에서 맞춤 코멘트를 받을 수 있습니다.” |
| `apps/web/app/events/[slug]/page.tsx:84–90` | `EventScheduled`, `isAccessibleForFree: true`, `organizer: noonch-i` | **오류:** 역사 사건 소개를 서비스가 주최하는 예정 행사로 표현 | 소개 문서에 맞는 `Article`로 변경; 허위 행사 상태·주최자·입장료 속성 삭제 |
| `apps/web/components/FeedbackWidget.tsx:105` | “전송됐습니다. 감사합니다!” | 실패에도 성공 표시 가능: 같은 파일 `24–29`; DB 실패도 성공 반환: `apps/web/app/api/feedback/route.ts:12–20` | 저장 성공 때만 “의견이 접수되었습니다.” 실패 시 “접수하지 못했습니다. 다시 시도해 주세요.” |
| `apps/web/app/(landing)/page.tsx:682`; `apps/web/app/contact/page.tsx:51`; `apps/web/app/privacy/page.tsx:14,18,22,34,38` | 익명화·외부 비전송·암호화·인증 설명 | 실제 처리와 다수 불일치 | **6번 표의 문장으로 교체** |

“실시간”, “AI가 100%…”라는 사용자용 주장은 발견하지 못했다. “검증된 일자”(`apps/web/app/(landing)/page.tsx:46`)의 개별 검수 증빙은 이번 감사에서 검증하지 않았다. 팀 규모·법인 실체를 입증하는 정보도 없으며, 푸터 “회사”(`SiteFooter.tsx:161`)는 “문의·지원”이 적절하다.

웨이트리스트 UI는 없지만 `README.md:51,55,73,107`, `docs/decisions/0001-mvp-scope.md:13,16,30`, `packages/shared/src/types.ts:181`에 문서·타입 잔재가 있다. `scripts/migrate.ts:126`은 기능 폐기를 명시한다.

**2. 브랜드 표기 인벤토리**

사용자 노출 문자열·메타·메일 전체와 지정 기준 문서의 표기를 정리했다. 패키지 import의 반복은 사용자용 표기로 세지 않았다.

| 표기 | 실제 위치 |
|---|---|
| `nunch●i` | `apps/web/components/NoonchiLogo.tsx:95–112` — JSX와 원형 요소를 합친 시각 표기 |
| `nunch-i` | `apps/web/components/NoonchiLogo.tsx:44` — 접근성 이름 |
| `noonch●i` | `apps/web/components/SiteFooter.tsx:20–31` — 저작권 영역 사용처 `90,187`; `apps/web/app/opengraph-image.tsx:52–63` |
| `noonch-i` | `apps/web/app/layout.tsx:26,27,33,35–38,45,46,51`; `apps/web/app/privacy/page.tsx:7`; `apps/web/app/terms/page.tsx:12`; `apps/web/app/admin/page.tsx:24`; `apps/web/app/events/[slug]/page.tsx:90`; `apps/web/app/opengraph-image.tsx:4`; `apps/web/components/result-card/ResultCard.tsx:261`; `apps/web/lib/mailer.ts:30`; `apps/web/app/api/admin/inquiries/reply/route.ts:27,32` |
| `Noonchi` | `apps/web/app/(landing)/page.tsx:429,450`; `CLAUDE.md:1,3,66`; `README.md:1,23` |
| `noonchi` | 메일 제목: `apps/web/app/api/contact/route.ts:45`, `apps/web/app/api/admin/inquiries/reply/route.ts:23` |
| `눈치` / `눈치(noonch-i)` | `apps/web/app/layout.tsx:33`; `apps/web/app/terms/page.tsx:12,15`; `CLAUDE.md:1,3`; `README.md:1` |
| `Nunchi` | `docker-compose.yml:1`의 개발 주석. 감사한 사용자 화면 문자열에는 없음 |
| 이전 도메인 | `nunchi-bay.vercel.app`: `apps/web/app/layout.tsx:21`, `privacy/page.tsx:8`, `events/page.tsx:12`, `events/[slug]/page.tsx:32,90`, `opengraph-image.tsx:142`, `robots.ts:3`, `sitemap.ts:4` |
| `nunch-i.com` | 소유자가 제공한 도메인. 위 UI·메타에는 반영되지 않음 |

내부 식별자도 `NoonchiLogo`, `@noonchi/shared`, `@noonchi/llm`, `noonchi-memo-*`로 남아 있다. 브랜드 결정과 코드 식별자 변경은 별도 범위다.

| 후보 | 장점 | 비용·주의점 |
|---|---|---|
| `nunch-i` | 도메인·현재 로고와 일치 | 하이픈 구두 전달이 번거롭고 ‘눈치’ 발음이 바로 전달되지 않을 수 있음 |
| `noonch-i` | `noon`이 ‘눈’의 발음을 연상시키기 쉬움 | 도메인·로고와 불일치, 기존 변형 유지 비용 |
| `Nunchi` | 짧고 하이픈 없이 쓰기 편함 | 영문 발음 편차, 동명 검색 경쟁 확인 필요 |
| `눈치` | 한국어 이해·발음이 즉각적 | 일반 명사라 검색 구별이 어려움; 영문 주소 병기 필요 |

검색량·상표·동명 서비스 조사는 미실시했다. **최종 명칭은 결정하지 않는다.**

**3. 페르소나·톤 일관성**

`CLAUDE.md:32–33`은 대기업 팀·AE가 우선이고, `docs/decisions/0001-mvp-scope.md:11–13`은 1인 PMM·무가입 검토다. 랜딩은 “마케터 한 명”(`:38,429`)에게 말하면서 “법무·홍보·임원 다단계 회의”(`:38`)와 내부 법무팀(`:674`)을 기본 전제로 둔다.

**권장 목소리: 혼자 또는 소규모로 캠페인을 준비하는 실무자의 1차 검토 도우미.** 무가입·한 화면·개인화 선택이라는 실제 기능에 맞고, 대기업 사용자도 배제하지 않는다. “사고 예방 보장”보다 “놓친 맥락과 확인할 근거”를 약속한다.

설명은 `합니다`, 안내는 `해 주세요`, 버튼은 `검토하기`로 통일한다.

| 위치 | BEFORE → AFTER |
|---|---|
| `apps/web/app/calendar/CalendarClient.tsx:466` | “등록된 민감 이벤트가 없어요” → “등록된 사건이 없습니다” |
| `apps/web/app/onboarding/page.tsx:99–100` | “당신의 브랜드”, “당신의 컨텍스트” → “브랜드에 맞는 안내”, “업종·채널” |
| `apps/web/components/result-card/ResultCard.tsx:102` | “컨셉” → “콘셉트” |
| `apps/web/app/check/page.tsx:126` | “비주얼 키워드” → “시각 키워드”; “Enter로 추가” → “Enter 키로 추가” |
| `apps/web/lib/routes.ts:63` | “온보딩” → “맞춤 설정” — 현재 `NavigationMenu` 호출은 발견하지 못함 |
| `apps/web/app/onboarding/page.tsx:16`; `apps/web/app/mypage/page.tsx:16` | “OOH 광고” → “옥외 광고” |
| `apps/web/app/check/page.tsx:60`; `apps/web/app/contact/page.tsx:238` | “시도해주세요”, “입력해주세요” → “시도해 주세요”, “입력해 주세요” |

`캠페인`, `카피`, `콘셉트`, `재검토`, `사전 검토`를 기준 어휘로 권장한다. `캠페인·카피`는 유지하되 `인텔리전스·컨텍스트·PMF`의 사용자용 노출은 풀어 쓴다.

**4. 원칙 위반 문구**

| 위치 | BEFORE → AFTER |
|---|---|
| `packages/shared/src/types.ts:187,190,191` | F “회피 필수” → “우선 재검토 권고”; B “안전” → “낮은 우려”; A “최적 타이밍” → “긍정 연관” |
| `apps/web/components/result-card/ResultCard.tsx:101` | “즉각 회피 — 캠페인 재설계를 권고합니다” → “우선 재검토 권고 — 날짜·표현 조정을 검토해 주세요” |
| 같은 파일 `:102` | “재검토 필요 — 컨셉·카피를 수정하세요” → “재검토 권고 — 콘셉트·카피 조정을 고려해 주세요” |
| 같은 파일 `:103` | “일반 주의 — 특이사항 없음” → “일반 주의 — 분석 근거를 확인해 주세요” |
| 같은 파일 `:104–105` | “안전 — 진행 가능합니다” → “낮은 우려 — 확인한 범위에서 우려가 낮습니다”; “강력한 호재입니다” → “기념일과 긍정적 연관이 있습니다” |
| `apps/web/app/(landing)/page.tsx:564` | “F등급으로 위험을 막고” → “F등급은 우선 재검토할 내용을 안내하고” |
| `apps/web/app/calendar/CalendarClient.tsx:72,75` | “즉각회피”, “안전” → “우선 재검토”, “낮은 우려” |
| `apps/web/components/result-card/ResultCard.tsx:373` | “감지된 위험 단어” → “확인이 필요한 키워드” |

C는 분석 오류의 대체 등급이기도 하므로 “특이사항 없음”으로 해석시키면 안 된다(`packages/llm/src/claude.ts:145–168`). 사건·캘린더 등급은 캠페인 입력을 검토한 결과가 아니므로 “날짜 참고 등급”으로 구별한다.

사람을 “일베 사용자” 등으로 분류하거나 특정 진영을 지지하는 **고정 UI 문구는 발견하지 못했다.** 표현 상태 라벨과 제한적 미탐 안내(`ResultCard.tsx:89–91,413`)는 방향이 적절하다. 다만 활성 사전 0건과 정상 검토 후 미탐을 구분해야 한다.

`apps/web/app/terms/page.tsx:14`의 서비스 악용 금지는 캠페인 판단을 강제하는 것과 다르므로 자동 삭제 대상이 아니다. 반면 `:15`의 “반드시 내부 법무…”는 “필요에 따라 관련 담당자나 전문가의 검토를 권장합니다”로 완화한다. 생성 문구에도 적용되도록 `packages/llm/src/prompts/review-campaign.ts:34–38`을 함께 정렬하고, 개인화 프롬프트 `personalize-rationale.ts:46–50`에도 중립·비낙인 원칙을 추가할 필요가 있다.

**5. 랜딩 페이지 리라이트**

다음 표의 위치는 모두 **`apps/web/app/(landing)/page.tsx`**다.

| 섹션·줄 | BEFORE → AFTER | 구분 |
|---|---|---|
| Hero 보조 `:286` | “브랜드 안전 · 날짜 리스크 분석” → “마케터를 위한 캠페인 사전 검토” | 톤 개선(선택) |
| H1 `:297–298` | “광고 사고는 출시 전에 막아야 합니다.” → “출시 전, 날짜와 카피의 맥락을 확인하세요.” | 톤 개선(선택) |
| 설명 `:308–310` | “날짜 × 카피…AI가 즉시 분석합니다” → “날짜·캠페인명·카피를 관련 사건과 함께 검토합니다. F부터 A까지 등급과 확인할 근거를 살펴보세요.” | 사실 수정(필수) |
| CTA `:324,334` | “지금 무료로 검토하기” → “검토 화면 보기”; “리스크 캘린더 보기” → 유지 | 사실 수정(필수) |
| 보조 `:342,344,346` | “회원가입 불필요” → “기본 검토는 가입 없이”; “60+” → “사건·기념일 50건”; “5초 안에 결과” → “등급과 근거 확인” | 사실 수정(필수) |
| 문제 `:424–429` | “마케터가 놓치기 쉬운 날짜 리스크…Noonchi가 대신합니다” → “혼자 찾기 어려운 날짜의 맥락. 관련 사건과 주의할 표현을 모아, 검토할 지점을 안내합니다.” | 톤 개선(선택) |
| 비교표 `:36–39,445,450` | “Before / After Noonchi” → “직접 확인할 때 / 검토 도구를 쓰면”; “다단계 회의 → …5초…” → “검토 근거를 따로 정리 → 등급과 사유를 한 화면에서 확인” | 사실 수정(필수) |
| 작동 방식 1 `:45–48` | “한국 사건 60+ DB” → “1. 날짜와 카피 입력”; 기존 DB 설명 → “출시 날짜와 카피를 입력합니다. 캠페인명·시각 키워드도 추가할 수 있습니다.” | 사실 수정(필수) |
| 작동 방식 2 `:52–55` | “맥락 교차 검토 AI” → “2. 관련 맥락 검토”; 기존 설명 → “등록된 키워드를 확인하고, 필요한 경우 관련 사건을 AI로 검토합니다.” | 사실 수정(필수) |
| 작동 방식 3 `:59–62` | “대안 자동 제안” → “3. 근거를 보고 판단”; 기존 설명 → “등급과 검토 사유를 확인합니다. 대안이 제공되면 참고하고, 수정한 카피를 다시 검토합니다.” | 사실 수정(필수) |
| 등급 소개 `:558–564` | “위험과 호재를 동시에 봅니다” → “우려와 긍정적 연관을 함께 봅니다”; “위험을 막고…” → “등급은 검토 우선순위를 안내합니다. 집행 여부는 직접 판단합니다.” | 톤 개선(선택) |
| 등급 카드 `:67–71` | F “즉각 회피”, B “안전”, A “최적 타이밍” → 4번 공통 라벨. 날짜만 나열한 예시는 삭제하고 “실제 등급은 입력 문구에 따라 달라집니다” 추가 | 톤 개선(선택) |
| 사례 `:81,89,97,610–613,646` | “F/D/A등급” → “예시: F/D/A등급”; “클릭 한 번…” → “예시를 불러온 뒤 검토를 시작해 보세요”; “검토 결과 보기” → “이 예시로 검토하기” | 사실 수정(필수) |
| FAQ 정확도 `:673–674` | “60+…Gemini…” → “등록된 사건과 규칙·AI 분석을 사용합니다. 모든 위험을 찾거나 결과의 정확성을 보장하지는 않습니다.” | 사실 수정(필수) |
| FAQ 범위 `:677–678` | 기존 범주 설명 → “한국 사건·기념일 50건을 바탕으로 검토합니다. 실시간 여론 전체를 반영하는 서비스는 아닙니다.” | 사실 수정(필수) |
| FAQ 학습 `:681–682` | 질문 “내 캠페인 데이터가 학습에 사용되나요?” → “입력한 데이터는 어떻게 처리되나요?”; 답변 → “입력 내용은 분석 서버로 전송되고 DB에 저장됩니다. 처리 범위와 보관 정책은 개인정보처리방침에서 확인할 수 있습니다.” | 사실 수정(필수) |
| FAQ F `:685–686` | 기존 답변 → “F는 우선 재검토를 권하는 등급입니다. 날짜·톤·표현 조정을 검토하고, 관련 근거를 바탕으로 결정해 주세요. 대안은 결과에 따라 제공됩니다.” | 사실 수정(필수) |
| FAQ 요금·글로벌 `:689–694` | 베타 무료·혜택·국가 순서 약속 → “현재 공개 서비스는 중단되어 있습니다. 재개 시 이용 조건을 안내합니다.” / “현재 검토 범위는 한국입니다. 추가 국가 지원은 확정되지 않았습니다.” | 사실 수정(필수) / 일정 확인 필요 |
| 마지막 CTA `:774,781,791` | “다음 캠페인이 사고가 되기 전에” → “다음 캠페인, 확인할 근거부터”; “지금 베타 무료…” → 운영 중단 안내; “무료로 검토 시작하기” → “검토 화면 보기” | 사실 수정(필수)·톤 개선 |

재개 전 공개 안내에서는 작동하지 않는 검토 CTA를 활성 서비스처럼 제공하지 않는다. 위 기능 카피는 로컬 미리보기·재개 시 사용할 초안이다.

**표현 리스크 신규 섹션** — 기존 문자열 없음. `apps/web/app/(landing)/page.tsx:550` 앞 삽입 제안:

> **날짜뿐 아니라, 표현의 맥락도 — 준비 중**  
> 커뮤니티 은어, 성별·지역 비하로 쓰일 수 있는 표현을 검토하는 기능을 준비하고 있습니다. 표현 사전 승인 후 제공 범위를 안내합니다.

승인·활성화 후에는 다음으로 교체한다.

> **같은 표현도, 쓰임에 따라 다릅니다.**  
> 커뮤니티 은어와 성별·지역 비하 표현을 등록된 사전과 문맥으로 검토합니다. 사람의 성향이나 소속은 판단하지 않습니다.

| 내부 tier | 사용자 설명 |
|---|---|
| `hard` | **재검토 권고:** 명백한 비하 용례로 등록된 표현입니다. 전체 등급은 D 또는 F가 됩니다. |
| `contextual` | **문맥 확인:** 음식·지명·방언 등 정상 용법과 구분합니다. 판단에 따라 재검토·문맥 확인을 권고하며, 등급은 바꾸지 않습니다. |
| `watch` | **참고:** 의미·기원에 논쟁이 있어 참고 정보를 제공합니다. 등급은 바꾸지 않습니다. |

추가 FAQ: “방언도 위험 표현인가요?” → “방언이라는 이유만으로 위험으로 분류하지 않습니다. 억양·의도나 모든 변형 표현을 판별하지는 못합니다.”

`apps/web/app/check/page.tsx:75`에는 현재 “표현 리스크 검토는 사전 승인 후 제공됩니다”를 추가한다. 기본 승인 필터는 `apps/web/lib/expression-lexicon.ts:45–58`, 미리보기 기본값은 `docker-compose.yml:39`에 있다. 세 사전의 상태 필드는 각각 `data/lexicon/{community,gender,region}.json:8`부터 확인되며 전 항목을 집계했다.

푸터 `apps/web/components/SiteFooter.tsx:148–149`:

> “브랜드 안전 인텔리전스…60+…5초…” → “캠페인 출시 전, 관련 사건과 표현의 맥락을 확인하는 도구입니다. 결과는 참고 정보이며, 모든 위험의 발견이나 캠페인의 안전을 보장하지 않습니다.”

**6. 법적·개인정보 문구 점검**

현재 흐름은 **입력 → 규칙 확인 → 필요한 경우 자체 운영 원격 AI 서버 → 원문 포함 DB 캐시**다. 카피는 임베딩 요청에도 전달된다(`apps/web/lib/review-engine.ts:53`; `packages/llm/src/claude.ts:173–176`). 로그인한 사용자에게는 업종·채널과 검토 결과를 이용한 추가 AI 호출이 있다.

| 위치·현재 주장 | 실제 차이 | 교체 문장 |
|---|---|---|
| `apps/web/app/privacy/page.tsx:14` “프로필 사진은 저장하지 않습니다” | 사진 **URL**을 저장: `apps/web/auth.ts:35–40`; `users.repo.ts:62–71` | “Google 로그인 시 계정 식별자, 이메일, 이름, 프로필 사진 URL을 저장합니다.” |
| 같은 파일 `:14` 브랜드 정보로 맞춤화 | 회사·브랜드·제품명은 저장하지만 현재 개인화 입력에는 없음 | “업종·채널은 맞춤 코멘트 생성에 사용합니다. 선택한 회사명·브랜드명·제품명은 프로필에 저장합니다.” |
| 같은 파일 `:18` “모든 데이터는 Supabase”, SOC 2·AES-256 | 현재 로컬 개발 구성과 불일치. 인증·디스크 암호화 적용도 입증 안 됨 | “현재 개발 환경은 PostgreSQL을 사용합니다. 캠페인 입력값은 애플리케이션에서 별도 암호화 없이 저장됩니다.” |
| 같은 파일 `:18,34` TLS 1.3·모든 통신 HTTPS·HTTP 리다이렉트 | 자체 AI 서버 통신은 HTTP. DB도 로컬 `PGSSL=disable` | “현재 개발 환경의 AI 서버 전송 구간에는 HTTPS 암호화가 적용되어 있지 않습니다.” **공개 재개 전 기술 조치 필요** |
| 같은 파일 `:22` “Google Gemini API에 전송” | 현재 자체 운영 모델. `GEMINI_API_KEY` 설정 시 Gemini 경로 선택 | “AI 분석이 필요한 경우 날짜·캠페인명·카피·시각 키워드를 자체 운영 분석 서버로 전송합니다.” 외부 API 활성화 시 실제 공급자·항목·정책을 다시 고지 |
| 같은 파일 `:22` “무료/유료 tier 모두…학습에 사용되지 않습니다” | **공식 약관과 불일치** | 해당 단정 삭제. “외부 AI 이용 시 데이터 처리 조건은 공급자와 적용 계약에 따라 달라집니다.” |
| `apps/web/app/(landing)/page.tsx:682` “익명화되어…저장” | 해시는 조회 키일 뿐, 원문도 저장: `apps/web/lib/review-engine.ts:192–206` | “검토 입력과 결과는 응답 재사용을 위해 DB에 저장됩니다. 입력 내용에서 식별 정보를 자동으로 제거하지 않습니다.” |
| 같은 곳; `apps/web/app/privacy/page.tsx:22` “회사명…외부로 전송되지 않습니다” | 별도 프로필 필드는 제외하지만 카피·캠페인명에 쓰면 전송 가능 | “별도 프로필의 회사명·브랜드명·제품명은 AI 요청에 포함하지 않습니다. 다만 검토 입력에 직접 적은 내용은 분석 서버로 전송될 수 있습니다.” |
| `apps/web/app/contact/page.tsx:51` “절대 공유되지 않습니다” | 분석 서버 전송과 조건부 외부 API 경로를 감춤 | “카피는 검토 처리에 사용되며 분석 서버로 전송됩니다. 자세한 처리 범위는 개인정보처리방침을 확인해 주세요.” |
| `apps/web/app/privacy/page.tsx:30` 보관·삭제 설명 | **7일은 캐시 유효기간이지 삭제 보장 아님.** 조회만 만료 검사: `reviews.repo.ts:24` | “검토 입력과 결과의 캐시 유효기간은 7일입니다. 유효기간 만료가 원문 자동 삭제를 뜻하지는 않습니다.” 실제 보유·삭제 기한 확정 필요 |
| 같은 파일 `:34` RLS·모든 관리자 조회 감사 로그 | 현재 마이그레이션은 RLS 없이 구성(`scripts/migrate.ts:23–29`); 조회 감사 로그 구현 미발견 | “관리자 기능은 로그인과 관리자 권한 확인을 거쳐 접근합니다.” 추가 보안 보장은 실측 후 기재 |
| 같은 파일 `:38` “Supabase Auth 세션 쿠키” | NextAuth JWT 세션: `apps/web/auth.config.ts:24–25` | “Google 로그인과 로그인 상태 유지를 위해 인증용 쿠키를 사용합니다.” |
| 같은 파일 `:14,38` Plausible 사용·준법 단정 | 사용 구현 미발견. 루트에는 Analytics TODO만 있음(`layout.tsx:86`) | Plausible·GDPR/PIPL/CCPA 문장 삭제. 서버 접속 로그 항목·기간은 **(확인 필요)** |
| 같은 파일 `:14,26` 문의·피드백 처리 누락 | 문의는 DB 저장 후 Gmail SMTP 알림; 피드백도 DB 저장 | “문의의 이름·이메일·내용은 DB에 저장되며 Gmail을 통해 관리자 알림과 답변에 사용됩니다. 피드백 유형·내용도 저장됩니다.” |
| `apps/web/app/terms/page.tsx:12,15` 결과 모두 AI 기반·법적 책임 전면 부인 | 규칙 결과도 존재. 전면 면책의 유효성은 코드로 판단 불가 | “검토 결과는 등록 데이터와 규칙·AI 분석에 기반한 참고 정보입니다. 최종 집행 전 관련 근거를 확인해 주세요.” **면책 범위는 변호사 검토** |

Gemini는 한국 이용을 전제로 무료·유료 조건을 동일하게 단정할 수 없다. 현재 공식 약관은 비과금 서비스의 입력·응답 활용과 유료 서비스의 처리 조건을 구분한다. [Google 공식 약관](https://ai.google.dev/gemini-api/terms)

**변호사·운영자 확인 필요:** 처리자·보호책임자, 실제 보유기간과 파기, Gmail 및 외부 AI의 위탁·국외이전 해당 여부, 문의 수집 고지, 법정 보관 근거, 권리 요청 처리기한, 포괄 면책, 이용 지속에 의한 약관 동의(`terms/page.tsx:16`). 현재의 “30일 이내 삭제”, “14일 이내 응답”, “7일/30일 전 고지”(`privacy/page.tsx:30,42,46`)는 이행 체계가 확인되지 않았다. 보유기간·위탁 등은 처리방침에 실제 내용을 명시해야 한다. [개인정보 보호법 제30조](https://www.law.go.kr/LSW/lsJoLnkSeq=1033215151)

**7. SEO·메타데이터**

현재 루트 메타가 대부분의 페이지에 적용된다. 개별 메타는 `apps/web/app/events/page.tsx:9`, `events/[slug]/page.tsx:25`, `privacy/page.tsx:5`에서 확인했다. 나머지 지정 페이지에는 개별 선언이 없다. 사건 상세 설명은 이름에 요약 140자를 덧붙여 155자를 넘길 수 있다(`events/[slug]/page.tsx:31`).

아래는 **브랜드 확정 전 사용할 수 있는 구체 문안**이다. `title`은 60자, `description`은 155자 이내이며 OG·Twitter도 같은 문장을 사용한다.

| 페이지 | title | description |
|---|---|---|
| `/` 현재 | 캠페인 사전 검토 서비스 안내 | 캠페인의 날짜와 카피를 관련 사건·기념일과 검토하는 도구입니다. 현재 공개 서비스는 중단되어 있으며, 표현 리스크 기능은 준비 중입니다. |
| `/check` | 캠페인 검토｜날짜와 카피의 맥락 확인 | 날짜·캠페인명·카피를 입력해 관련 사건과 검토 근거를 확인합니다. 기본 검토는 가입 없이 이용하도록 설계되어 있습니다. |
| `/calendar` | 리스크 캘린더｜캠페인 기획 참고일 | 한국 사건·기념일을 월별로 살펴보고 관련 맥락을 확인합니다. 날짜 참고 등급은 개별 캠페인의 검토 결과와 다를 수 있습니다. |
| `/events` | 리스크 라이브러리｜한국 사건·기념일 | 캠페인 검토에 사용하는 한국 사건·기념일 50건의 배경, 주의 키워드, 권장 톤과 출처를 확인합니다. |
| `/events/[slug]` 예시 | 5·18 광주민주화운동｜캠페인 기획 참고 | 5·18 광주민주화운동의 배경과 캠페인 기획 시 확인할 키워드·권장 톤·출처를 살펴봅니다. |
| `/contact` | 문의하기｜질문·제안·오류 제보 | 서비스 관련 질문, 기능 제안과 오류를 보낼 수 있는 문의 페이지입니다. 이름·이메일·문의 내용의 처리 안내를 확인해 주세요. |
| `/privacy` | 개인정보처리방침｜수집·이용·보관 안내 | 로그인 정보, 캠페인 입력, 문의와 피드백의 수집·이용·전송·보관 및 권리 행사 방법을 안내합니다. |
| `/terms` | 이용약관｜서비스 이용 조건 | 캠페인 검토 서비스의 제공 범위, 참고 정보의 한계와 이용 조건을 안내합니다. |
| `/onboarding` | 맞춤 설정｜업종·채널 등록 | 업종과 주요 채널을 저장해 다음 캠페인 검토에서 맞춤 코멘트를 받을 수 있습니다. 기본 검토에는 설정이 필요하지 않습니다. |
| `/mypage` | 내 정보｜맞춤 설정 관리 | 업종·채널과 선택한 브랜드 정보를 확인하고 수정합니다. |
| `/auth/error` | 로그인 오류｜다시 이용하기 | Google 로그인 중 문제가 발생했습니다. 홈으로 돌아가 다시 시도하거나 기본 캠페인 검토를 이용할 수 있습니다. |

개인 페이지·인증 오류·관리자는 `noindex` 권장. 사건 상세는 완성된 제목·설명 전체 길이를 제한한다. 도메인은 브랜드 선택과 별개로 소유자가 제시한 `https://nunch-i.com`에 맞춰 canonical·OG URL·sitemap·robots를 정렬하되, 재개·연결 상태를 확인한 뒤 공개한다. OG 이미지의 문자열도 별도 교체해야 한다.

**8. 적용 우선순위 — 구현 전달용**

P0는 확인된 오류만 포함한다. 미확정 사업 계획·보안 보장·법률 해석은 확인 과제로 분리한다.

**P0 — 사실 오류·실제 처리 불일치**

- `apps/web/app/(landing)/page.tsx:29,45,47,344,674` — `60+` → `50건`; 1번 표의 메타·검토·푸터·OG 반복 위치도 일괄 적용.
- `apps/web/app/(landing)/page.tsx:30,38,54,346`; `apps/web/app/check/page.tsx:74,227` — `5초 / 5–10초` → `사전 검토 / 약 10–20초, 더 길어질 수 있음`; 1번 표의 반복 위치 포함.
- `apps/web/app/(landing)/page.tsx:674,678` — `Gemini LLM / 5가지 카테고리` → `규칙·AI 분석 / 역사·사회 사건과 기념일`.
- `apps/web/app/(landing)/page.tsx:60,686` — `대안이 함께 제시됩니다` → `대안은 결과에 따라 제공됩니다`.
- `apps/web/app/(landing)/page.tsx:613,646` — `클릭 한 번…결과 / 검토 결과 보기` → `예시를 불러온 뒤 검토 / 이 예시로 검토하기`.
- `apps/web/app/terms/page.tsx:13`; `apps/web/app/(landing)/page.tsx:690,781` — `현재 베타 운영 / 지금 베타 무료` → `현재 공개 서비스 중단`.
- `apps/web/app/privacy/page.tsx:14,18,22,34,38`; `apps/web/app/(landing)/page.tsx:682` — `사진 미저장·Supabase·HTTPS 전체 보장·익명화·학습조건 동일·Supabase Auth` → 6번 표의 실제 처리 문장.
- `apps/web/app/mypage/page.tsx:107` — `모든 검토 결과…즉시 반영` → `다음 검토에서 업종·채널에 맞춘 코멘트 제공`.
- `apps/web/app/events/[slug]/page.tsx:84–90` — `EventScheduled·서비스 주최자` → 소개 문서 `Article`.
- `apps/web/components/FeedbackWidget.tsx:105` — 무조건 `전송됐습니다` → 저장 성공 시 `접수되었습니다`; 실패 분기 구현 필요.

**P1 — 원칙·톤·고지 보완**

- `packages/shared/src/types.ts:187,190,191` — `회피 필수 / 안전 / 최적 타이밍` → `우선 재검토 권고 / 낮은 우려 / 긍정 연관`; ResultCard·랜딩·캘린더·생성 프롬프트 동기화.
- `apps/web/components/result-card/ResultCard.tsx:102–105` — `수정하세요·특이사항 없음·진행 가능합니다` → 4번 권고형 문장.
- `apps/web/components/result-card/ResultCard.tsx:413` — 활성 사전 0건에서도 `추가 위험을 찾지 못했습니다` → 해당 상태에서는 `표현 리스크 검토는 준비 중입니다`.
- `apps/web/components/AppHeader.tsx:167,298` — `무료로 시작하기` → `Google로 로그인`.
- `apps/web/app/privacy/page.tsx:30` — 보관기한 설명 부재 → `캐시 유효기간 7일과 원문 삭제를 구분`; 실제 파기 정책 확정.
- `apps/web/app/terms/page.tsx:15` — `반드시 내부 법무…` → `필요에 따라 담당자나 전문가의 검토를 권장합니다`; 면책은 변호사 검토.

**P2 — 카피·발견성 개선**

- `apps/web/app/(landing)/page.tsx:297–310,45–62` — 사고 방지·기능 나열 → 5번 Hero·3단계 문안.
- `apps/web/app/(landing)/page.tsx:550` 앞; `apps/web/app/check/page.tsx:75` — 표현 기능 설명 없음 → `준비 중` 안내; 승인 후 제공 문구로 전환.
- `apps/web/components/SiteFooter.tsx:148–149` — 안전·속도 홍보 → 범위와 한계가 명확한 푸터 문안.
- `apps/web/app/layout.tsx:26–52` 및 개별 페이지 — 공통 홍보 메타 → 7번 페이지별 문안.
- `apps/web/app/layout.tsx:21`; `apps/web/app/robots.ts:3`; `apps/web/app/sitemap.ts:4` — 이전 도메인 → `https://nunch-i.com`, 공개 연결 확인 후 적용.
- 브랜드 인벤토리의 모든 사용자 노출 위치 — 혼재 표기 → **소유자가 선택한 `{BRAND}`**. 명칭 결정 전 일괄 치환하지 않음.
